# How to run

install [probe.rs](https://probe.rs) `cargo run`

# TODO

The working list lives in [TODO.md](TODO.md), which collects every outstanding item — the ones that
used to be here plus the `//TODO` comments in `src/` — with source line references and a suggested
priority order.
